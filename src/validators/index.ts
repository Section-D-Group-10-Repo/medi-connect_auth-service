import * as authValidator from "./auth.validator";

import * as queryValidator from "./query.validator";

import * as userValidator from "./user.validator";

export { queryValidator, authValidator, userValidator };
